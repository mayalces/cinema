<p id="notice"><%= notice %></p>

<p>
  <strong>Movie:</strong>
  <%= Movie.find(MovieShowtime.find(@reservation.movie_showtime_id).movie_id).name %>
</p>

<p>
  <strong>Seatroom:</strong>
  <%= SeatRoom.find(@reservation.seat_room_id).seat.to_s+SeatRoom.find(@reservation.seat_room_id).line %>
</p>

<p>
  <strong>User:</strong>
  <%= User.find(@reservation.user_id).email %>
</p>

<p>
  <strong>Food:</strong>
  <%= Food.find(@reservation.food_id).name %>
</p>

<%= link_to 'Edit', edit_reservation_path(@reservation) %> |
<%= link_to 'Back', reservations_path %>

<%= form_for(@seat_room) do |f| %>
  <% if @seat_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seat_room.errors.count, "error") %> prohibited this seat_room from being saved:</h2>

      <ul>
      <% @seat_room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :seat %><br>
    <%= f.number_field :seat %>
  </div>
  <div class="field">
    <%= f.label :line %><br>
    <%= f.text_field :line %>
  </div>
  <div class="field">
    <%= f.label :state %><br>
    <%= f.text_field :state %>
  </div>
  <div class="field">
    <%= f.label :movie_room_id %><br />
    <%= f.select :movie_room_id, options_for_select(MovieRoom.all.map{|s|[s.id, s.id]}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
